'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export function PrescriptionUpload(){const [busy,setBusy]=useState(false);const [error,setError]=useState('');const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const fd=new FormData(e.currentTarget);const res=await fetch('/api/prescriptions',{method:'POST',body:fd});const body=await res.json().catch(()=>({}));if(!res.ok){setError(body.message??'Upload failed');setBusy(false);return;}e.currentTarget.reset();setBusy(false);router.refresh()}return <form className="form-grid" onSubmit={submit}><div className="field"><label htmlFor="prescription">Prescription image or PDF</label><input id="prescription" name="file" type="file" required accept="image/jpeg,image/png,application/pdf"/><span className="small muted">JPG, PNG or PDF. Maximum 8 MB.</span></div>{error&&<div className="alert">{error}</div>}<button className="btn btn-primary" disabled={busy}>{busy?'Uploading…':'Upload prescription'}</button></form>}
