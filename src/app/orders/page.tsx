import { redirect } from 'next/navigation';
import { currentUser } from '@/lib/auth';
import { prisma } from '@/lib/prisma';
import { formatInr } from '@/lib/money';
import { LogoutButton } from '@/components/LogoutButton';
export default async function OrdersPage(){const user=await currentUser();if(!user)redirect('/login?next=/orders');const orders=await prisma.order.findMany({where:{userId:user.id},include:{items:true,statusHistory:{orderBy:{createdAt:'asc'}}},orderBy:{createdAt:'desc'}});return <main className="container"><div className="page-head"><div style={{display:'flex',justifyContent:'space-between',gap:16,alignItems:'end'}}><div><span className="eyebrow">Account</span><h1 style={{fontSize:42}}>Orders</h1><p className="muted">Signed in as {user.email}</p></div><LogoutButton/></div></div><div className="list">{orders.length?orders.map(o=><article className="panel" key={o.id}><div style={{display:'flex',justifyContent:'space-between',gap:12,flexWrap:'wrap'}}><div><h3>{o.publicId}</h3><span className="small muted">Placed {o.createdAt.toLocaleString('en-IN')}</span></div><div><span className={`status ${o.status}`}>{o.status.replaceAll('_',' ')}</span> <b style={{marginLeft:10}}>{formatInr(o.totalPaise)}</b></div></div><div style={{marginTop:14}}>{o.items.map(i=><div className="summary-line" key={i.id}><span>{i.nameSnapshot} × {i.quantity}</span><span>{formatInr(i.unitPricePaise*i.quantity)}</span></div>)}</div><div style={{borderTop:'1px solid var(--line)',marginTop:12,paddingTop:12}}><b>Tracking</b><div className="small muted">{o.statusHistory.map(h=>`${h.status.replaceAll('_',' ')} · ${h.createdAt.toLocaleString('en-IN')}`).join('  →  ')}</div></div></article>):<div className="panel">No orders yet.</div>}</div></main>}
